The DMZ Platform is a component bases game engine. The platform contains frameworks that provide functionality such as rendering, audio, and user interface. The platform may be used to rapidly develop applications by extensively reusing components found in the provided frameworks.